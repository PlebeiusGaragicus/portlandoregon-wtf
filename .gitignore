node_modules/
dist/
.env
.DS_Store
*.log

# Raw/intermediate GIS extracts (MANIFEST.json and endpoints.json stay committed)
data/raw/
data/processed/
data/maps/

# Map assets staged into the client for the serverless build (~30 MB).
# Populated by scripts/stage-map.sh from data/maps/ or the map release.
client/src/public/map/
